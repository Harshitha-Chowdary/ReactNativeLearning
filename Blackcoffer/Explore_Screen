import React from 'react';
import { View, Text, ScrollView, TouchableOpacity, StyleSheet } from 'react-native';

const UserProfileCard = ({ name, role, location, distance, bio }) => (
  <View style={styles.userCard}>
    <Text style={styles.userName}>{name}</Text>
    <Text style={styles.userInfo}>{role} | {location} | Within {distance} KM</Text>
    <Text style={styles.userBio}>{bio}</Text>
  </View>
);

const App = () => {
  return (
    <ScrollView style={styles.container}>
      <UserProfileCard
        name="Naveena Bathula"
        role="Business | Friendship"
        location="Tirupati"
        distance="3.9"
        bio="Hi community! I am open to new connections. I am an enthusiastic person who believes that you have to work hard for what you want to achieve in your life. I am a fresher with strong..."
      />

      <UserProfileCard
        name="Hithesh Nukala"
        role="Coffee | Business | Friendship"
        location="Tirupati"
        distance="4.0"
        bio="Hi community! I am open to new connections."
      />

      <UserProfileCard
        name="Naga Charan Tangella"
        role="Student"
        location="Tirupati"
        distance="0"
        bio=""
      />
    </ScrollView>
  );
};

const styles = StyleSheet.create({
  container: {
    padding: 20,
  },
  userCard: {
    backgroundColor: '#fff',
    borderRadius: 10,
    padding: 15,
    marginBottom: 20,
  },
  userName: {
    fontSize: 18,
    fontWeight: 'bold',
    marginBottom: 5,
  },
  userInfo: {
    fontSize: 14,
    color: '#666',
    marginBottom: 10,
  },
  userBio: {
    fontSize: 14,
    color: '#333',
  },
});

export default App;